schema_version: "1.0"

extension:
  id: "progress"
  name: "Progress Hooks"
  version: "1.0.0"
  description: "Companion to the progress-report preset, covering the tasks/implement phases when a replace-strategy preset (e.g. explicit-task-dependencies, parse-dont-validate) clobbers the preset's wrap on those commands — only extensions can declare lifecycle hooks, so this extension supplies before_tasks / before_implement hooks that mark those two phases active regardless of who owns the command body. It owns no writer of its own: its commands resolve the preset's progress_report.py and no-op gracefully if the preset (or the dashboard repo) isn't present. Install alongside the progress-report preset."
  author: "imosquera"
  repository: "https://github.com/imosquera/speckit-squads"
  license: "MIT"

requires:
  speckit_version: ">=0.6.0"

provides:
  commands:
    - name: "speckit.progress.tasks"
      file: "commands/tasks.md"
      description: "Hook target for before_tasks: marks the tasks phase active on the dashboard card. Clobber-immune where a replaced /speckit-tasks body would drop the preset's wrap."
    - name: "speckit.progress.implement"
      file: "commands/implement.md"
      description: "Hook target for before_implement: marks the implement phase active on the dashboard card. Clobber-immune where a replaced /speckit-implement body would drop the preset's wrap."

hooks:
  before_tasks:
    command: "speckit.progress.tasks"
    optional: false
    description: "Mark the tasks phase active on the dashboard card (fires even when /speckit-tasks is replaced by another preset)."
    condition: null
  before_implement:
    command: "speckit.progress.implement"
    optional: false
    description: "Mark the implement phase active on the dashboard card (fires even when /speckit-implement is replaced by another preset)."
    condition: null

tags:
  - "progress"
  - "dashboard"
  - "agent-os"
  - "hooks"
  - "observability"
